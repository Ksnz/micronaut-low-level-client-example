# micronaut-low-level-client-example
Попытки создать 1->n бинов клиентов с единым интерфейсом из конфига

###Для запуска под виндой:
`gradlew run`

###Для запуска в иной среде можно попробовать:
`sh gradlew run`